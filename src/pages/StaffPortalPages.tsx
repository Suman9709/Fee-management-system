import type { ReactNode } from "react"
import { useMemo, useState } from "react"
import {
  Bell,
  CalendarDays,
  CheckCircle2,
  ClipboardCheck,
  Download,
  FileText,
  Mail,
  Megaphone,
  MessageCircle,
  Plus,
  ReceiptText,
  Search,
  Send,
} from "lucide-react"

const Card = ({ children, className = "" }: { children: ReactNode; className?: string }) => (
  <section className={`rounded-2xl border border-slate-200/80 bg-white shadow-[0_1px_2px_rgba(15,23,42,0.04)] ${className}`}>{children}</section>
)

const PageHeader = ({ title, description, action }: { title: string; description: string; action?: ReactNode }) => (
  <header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
    <div><p className="text-sm font-semibold text-blue-600">Office staff workspace</p><h2 className="mt-1 text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl">{title}</h2><p className="mt-2 max-w-2xl text-sm text-slate-500 sm:text-base">{description}</p></div>
    {action}
  </header>
)

const Badge = ({ value }: { value: string }) => <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${value === "Paid" || value === "Updated" || value === "Published" ? "bg-emerald-50 text-emerald-700" : value === "Overdue" ? "bg-rose-50 text-rose-700" : "bg-amber-50 text-amber-700"}`}>{value}</span>

const staffTasks = [
  { title: "Send Term 3 fee reminders", detail: "46 parents have pending payments", icon: Send, color: "bg-blue-50 text-blue-600" },
  { title: "Update monthly attendance", detail: "Grade 8–12 records are pending", icon: ClipboardCheck, color: "bg-amber-50 text-amber-600" },
  { title: "Publish September holiday", detail: "One draft is ready for review", icon: CalendarDays, color: "bg-violet-50 text-violet-600" },
]

export const StaffDashboardPage = () => (
  <div className="mx-auto max-w-7xl space-y-7">
    <PageHeader title="Good morning, Kavita" description="Manage fee communication, attendance records, and school-wide updates." action={<button className="inline-flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3.5 py-2.5 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-50"><Download className="size-4" /> Download daily report</button>} />
    <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Today’s collections", "₹1,14,650", "12 confirmed payments"], ["Pending fees", "₹1.32L", "74 invoices need follow-up"], ["Attendance updates", "6 classes", "September records pending"], ["Messages sent", "1,149", "96.8% delivered today"]].map(([label, value, detail]) => <Card key={label} className="p-5"><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-slate-900">{value}</p><p className="mt-1.5 text-xs text-slate-400">{detail}</p></Card>)}</div>
    <div className="grid gap-6 xl:grid-cols-5"><Card className="xl:col-span-3"><div className="border-b border-slate-100 px-5 py-5 sm:px-6"><h3 className="text-base font-semibold text-slate-900">Office action queue</h3><p className="mt-1 text-sm text-slate-500">Important tasks for today</p></div><div className="divide-y divide-slate-100">{staffTasks.map(({ title, detail, icon: Icon, color }) => <div key={title} className="flex items-center gap-4 px-5 py-4 sm:px-6"><span className={`flex size-10 shrink-0 items-center justify-center rounded-xl ${color}`}><Icon className="size-5" /></span><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-slate-800">{title}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></div><button className="text-sm font-semibold text-blue-600 hover:text-blue-700">Open</button></div>)}</div></Card><Card className="p-5 sm:p-6 xl:col-span-2"><div className="flex items-start justify-between"><div><h3 className="text-base font-semibold text-slate-900">Communication status</h3><p className="mt-1 text-sm text-slate-500">Current delivery activity</p></div><Bell className="size-5 text-slate-400" /></div><div className="mt-6 space-y-4">{[["Fee reminders", "274 delivered", "bg-blue-600"], ["Holiday notice", "1,207 delivered", "bg-violet-500"], ["Attendance update", "842 delivered", "bg-emerald-500"]].map(([label, value, color]) => <div key={label}><div className="flex justify-between text-sm"><span className="font-medium text-slate-700">{label}</span><span className="text-slate-500">{value}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${color}`} style={{ width: "92%" }} /></div></div>)}</div></Card></div>
  </div>
)

const studentSeeds = ["Aarav Sharma", "Diya Patel", "Vivaan Gupta", "Ananya Singh", "Ishaan Verma", "Myra Kapoor", "Reyansh Jain", "Kabir Mehta", "Sara Khan", "Aditya Nair"]
const officeStudents = Array.from({ length: 1248 }, (_, index) => ({ id: `ST-${1001 + index}`, name: studentSeeds[index % studentSeeds.length], grade: `Grade ${6 + (index % 7)}-${String.fromCharCode(65 + (index % 3))}`, guardian: `${["Rohan", "Kavita", "Amit", "Pooja"][index % 4]} Guardian`, status: index % 6 === 0 ? "Overdue" : index % 4 === 0 ? "Pending" : "Paid" }))

export const StaffStudentsPage = () => {
  const [query, setQuery] = useState("")
  const [page, setPage] = useState(1)
  const pageSize = 10
  const filteredStudents = useMemo(() => officeStudents.filter((student) => `${student.name} ${student.id} ${student.grade}`.toLowerCase().includes(query.toLowerCase())), [query])
  const totalPages = Math.max(1, Math.ceil(filteredStudents.length / pageSize))
  const activePage = Math.min(page, totalPages)
  const visibleStudents = filteredStudents.slice((activePage - 1) * pageSize, activePage * pageSize)
  return <div className="mx-auto max-w-7xl space-y-6"><PageHeader title="Student records" description="Search the school directory, review fee status, and open student records." /><Card><div className="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row sm:items-center sm:justify-between sm:p-5"><div className="relative w-full sm:max-w-sm"><Search className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => { setQuery(event.target.value); setPage(1) }} className="h-10 w-full rounded-lg border border-slate-200 bg-white pl-9 pr-3 text-sm outline-none focus:border-blue-500 focus:ring-3 focus:ring-blue-100" placeholder="Search name, ID, or grade" /></div><span className="text-sm text-slate-500">{filteredStudents.length.toLocaleString()} students</span></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold tracking-wide text-slate-500 uppercase"><tr><th className="px-5 py-3 sm:px-6">Student</th><th className="px-5 py-3">Grade</th><th className="px-5 py-3">Guardian</th><th className="px-5 py-3">Fee status</th><th className="px-5 py-3 sm:px-6">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{visibleStudents.map((student) => <tr key={student.id} className="hover:bg-slate-50/70"><td className="px-5 py-4 sm:px-6"><p className="font-semibold text-slate-800">{student.name}</p><p className="mt-0.5 text-xs text-slate-400">{student.id}</p></td><td className="px-5 py-4 text-slate-600">{student.grade}</td><td className="px-5 py-4 text-slate-600">{student.guardian}</td><td className="px-5 py-4"><Badge value={student.status} /></td><td className="px-5 py-4 sm:px-6"><button className="text-sm font-semibold text-blue-600 hover:text-blue-700">Open record</button></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t border-slate-100 px-5 py-3.5 text-sm text-slate-500 sm:px-6"><span>Showing {(activePage - 1) * pageSize + 1}–{Math.min(activePage * pageSize, filteredStudents.length)} of {filteredStudents.length.toLocaleString()}</span><div className="flex gap-2"><button disabled={activePage === 1} onClick={() => setPage((current) => current - 1)} className="rounded-md border border-slate-200 px-3 py-1.5 text-xs font-semibold disabled:opacity-40">Previous</button><button disabled={activePage === totalPages} onClick={() => setPage((current) => current + 1)} className="rounded-md border border-slate-200 px-3 py-1.5 text-xs font-semibold disabled:opacity-40">Next</button></div></div></Card></div>
}

const feeStructures = [
  { name: "Primary annual fee", grades: "Grades 1–5", amount: "₹32,000", instalments: "4 instalments", students: 394 },
  { name: "Middle school fee", grades: "Grades 6–8", amount: "₹38,000", instalments: "4 instalments", students: 428 },
  { name: "Senior school fee", grades: "Grades 9–12", amount: "₹46,000", instalments: "4 instalments", students: 426 },
]

export const StaffFeeSetupPage = () => {
  const [created, setCreated] = useState(false)
  return <div className="mx-auto max-w-7xl space-y-6"><PageHeader title="Fee setup" description="Configure class-wise fee structures, instalments, concessions, and collection due dates." action={<button onClick={() => setCreated(true)} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><Plus className="size-4" /> Create fee plan</button>} />{created && <div className="flex items-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700"><CheckCircle2 className="size-4" /> New fee plan draft created. It can now be assigned to a class.</div>}<div className="grid gap-5 lg:grid-cols-3">{feeStructures.map((plan) => <Card key={plan.name} className="p-5"><div className="flex items-start justify-between"><span className="flex size-10 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><FileText className="size-5" /></span><Badge value="Published" /></div><h3 className="mt-5 text-base font-bold text-slate-900">{plan.name}</h3><p className="mt-1 text-sm text-slate-500">{plan.grades}</p><p className="mt-5 text-2xl font-bold tracking-tight text-slate-900">{plan.amount}<span className="ml-1 text-sm font-medium text-slate-400">/ year</span></p><div className="mt-5 grid grid-cols-2 border-t border-slate-100 pt-4 text-sm"><div><p className="text-slate-400">Instalments</p><p className="mt-1 font-semibold text-slate-700">{plan.instalments}</p></div><div><p className="text-slate-400">Assigned</p><p className="mt-1 font-semibold text-slate-700">{plan.students} students</p></div></div><button className="mt-5 w-full rounded-lg border border-slate-200 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Edit fee plan</button></Card>)}</div><Card><div className="border-b border-slate-100 px-5 py-5 sm:px-6"><h3 className="text-base font-semibold text-slate-900">Collection rules</h3><p className="mt-1 text-sm text-slate-500">Rules automatically apply when invoices are generated.</p></div><div className="grid divide-y divide-slate-100 md:grid-cols-3 md:divide-x md:divide-y-0">{[["Late fee", "₹250 after 7 days"], ["Sibling concession", "10% for second child"], ["Reminder schedule", "7, 3, 0, +3, +7 days"]].map(([label, value]) => <div key={label} className="p-5 sm:p-6"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 font-semibold text-slate-800">{value}</p></div>)}</div></Card></div>
}

type StaffInvoice = { id: string; student: string; grade: string; amount: string; due: string; status: string }
const initialInvoices: StaffInvoice[] = [
  { id: "INV-2026-143", student: "Vivaan Gupta", grade: "Grade 12-A", amount: "₹22,000", due: "15 Sep 2026", status: "Overdue" },
  { id: "INV-2026-142", student: "Myra Kapoor", grade: "Grade 8-A", amount: "₹15,600", due: "15 Sep 2026", status: "Pending" },
  { id: "INV-2026-141", student: "Diya Patel", grade: "Grade 8-B", amount: "₹9,500", due: "15 Sep 2026", status: "Pending" },
]

export const StaffInvoicesPage = () => {
  const [invoices, setInvoices] = useState(initialInvoices)
  const [delivery, setDelivery] = useState<Record<string, string>>({})
  const generateInvoice = () => setInvoices((current) => [{ id: `INV-2026-${144 + current.length}`, student: "Aanya Sharma", grade: "Grade 6-B", amount: "₹9,500", due: "15 Sep 2026", status: "Pending" }, ...current])
  const sendInvoice = (id: string, channel: "Email" | "WhatsApp") => setDelivery((current) => ({ ...current, [id]: `${channel} sent` }))
  return <div className="mx-auto max-w-7xl space-y-6"><PageHeader title="Invoices & billing" description="Generate fee invoices, share them through Email or WhatsApp, and monitor payment status." action={<button onClick={generateInvoice} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><Plus className="size-4" /> Generate invoice</button>} /><div className="grid gap-4 sm:grid-cols-3">{[["Invoices generated", `${invoices.length + 1},248`, "Current-term invoices"], ["Due this week", "₹3.42L", "Across 184 invoices"], ["Delivered", "96.8%", "Email and WhatsApp"]].map(([label, value, detail]) => <Card key={label} className="p-5"><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-slate-900">{value}</p><p className="mt-1 text-xs text-slate-400">{detail}</p></Card>)}</div><Card><div className="border-b border-slate-100 px-5 py-5 sm:px-6"><h3 className="text-base font-semibold text-slate-900">Current invoices</h3><p className="mt-1 text-sm text-slate-500">Each invoice can be sent directly to the linked parent account.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[880px] text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold tracking-wide text-slate-500 uppercase"><tr><th className="px-5 py-3 sm:px-6">Invoice</th><th className="px-5 py-3">Student</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Due date</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 sm:px-6">Send invoice</th></tr></thead><tbody className="divide-y divide-slate-100">{invoices.map((invoice) => <tr key={invoice.id}><td className="px-5 py-4 font-semibold text-blue-600 sm:px-6"><span className="inline-flex items-center gap-1.5"><ReceiptText className="size-4" />{invoice.id}</span></td><td className="px-5 py-4"><p className="font-semibold text-slate-800">{invoice.student}</p><p className="mt-0.5 text-xs text-slate-400">{invoice.grade}</p></td><td className="px-5 py-4 font-bold text-slate-800">{invoice.amount}</td><td className="px-5 py-4 text-slate-600">{invoice.due}</td><td className="px-5 py-4"><Badge value={invoice.status} /></td><td className="px-5 py-4 sm:px-6"><div className="flex items-center gap-2"><button onClick={() => sendInvoice(invoice.id, "Email")} className="inline-flex items-center gap-1.5 rounded-lg border border-slate-200 px-2.5 py-2 text-xs font-semibold text-slate-600 hover:bg-slate-50"><Mail className="size-3.5" /> Email</button><button onClick={() => sendInvoice(invoice.id, "WhatsApp")} className="inline-flex items-center gap-1.5 rounded-lg bg-emerald-600 px-2.5 py-2 text-xs font-semibold text-white hover:bg-emerald-700"><MessageCircle className="size-3.5" /> WhatsApp</button>{delivery[invoice.id] && <span className="text-xs font-semibold text-emerald-600">{delivery[invoice.id]}</span>}</div></td></tr>)}</tbody></table></div></Card></div>
}

const pendingFeeRecords = [{ name: "Vivaan Gupta", grade: "Grade 12-A", guardian: "Amit Gupta", amount: "₹12,500", status: "Overdue" }, { name: "Myra Kapoor", grade: "Grade 8-A", guardian: "Sonia Kapoor", amount: "₹15,600", status: "Overdue" }, { name: "Ishaan Verma", grade: "Grade 10-B", guardian: "Neha Verma", amount: "₹7,200", status: "Pending" }]

export const StaffPaymentsPage = () => { const [sent, setSent] = useState<string[]>([]); return <div className="mx-auto max-w-6xl space-y-6"><PageHeader title="Payments & fee follow-up" description="Track fee collections and send payment reminders to guardians." action={<button onClick={() => setSent(pendingFeeRecords.map((record) => record.name))} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><Send className="size-4" /> Send all reminders</button>} /><div className="grid gap-4 sm:grid-cols-3">{[["Today’s collection", "₹1,14,650", "12 successful payments"], ["Pending fees", "₹1,32,400", "74 open invoices"], ["Reminders delivered", `${sent.length}`, "Messages sent today"]].map(([label, value, detail]) => <Card key={label} className="p-5"><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-slate-900">{value}</p><p className="mt-1 text-xs text-slate-400">{detail}</p></Card>)}</div><Card><div className="divide-y divide-slate-100">{pendingFeeRecords.map((record) => <div key={record.name} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div><p className="font-semibold text-slate-800">{record.name} <span className="font-normal text-slate-400">· {record.grade}</span></p><p className="mt-1 text-sm text-slate-500">Guardian: {record.guardian}</p></div><div className="flex items-center gap-4"><div><p className="text-xs text-slate-400">Outstanding</p><p className="mt-1 font-bold text-rose-600">{record.amount}</p></div><Badge value={record.status} /><button onClick={() => setSent((current) => current.includes(record.name) ? current : [...current, record.name])} className={`rounded-lg px-3 py-2 text-sm font-semibold ${sent.includes(record.name) ? "bg-emerald-50 text-emerald-700" : "bg-slate-900 text-white hover:bg-slate-800"}`}>{sent.includes(record.name) ? "Reminder sent" : "Send reminder"}</button></div></div>)}</div></Card></div> }

const monthlyAttendance = [{ grade: "Grade 6", students: 178, rate: "96.2%", status: "Updated" }, { grade: "Grade 8", students: 242, rate: "94.8%", status: "Updated" }, { grade: "Grade 10", students: 236, rate: "93.1%", status: "Pending" }, { grade: "Grade 12", students: 189, rate: "91.7%", status: "Pending" }]

export const StaffAttendancePage = () => { const [updated, setUpdated] = useState<string[]>([]); return <div className="mx-auto max-w-6xl space-y-6"><PageHeader title="Monthly attendance" description="Update the official monthly attendance summary for each grade." action={<div className="flex gap-2"><select className="rounded-lg border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700"><option>September 2026</option><option>August 2026</option></select><button className="rounded-lg border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700 hover:bg-slate-50">Import attendance</button></div>} /><Card><div className="border-b border-slate-100 px-5 py-5 sm:px-6"><h3 className="text-base font-semibold text-slate-900">September attendance register</h3><p className="mt-1 text-sm text-slate-500">Records are published to parent and admin portals once updated.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold tracking-wide text-slate-500 uppercase"><tr><th className="px-5 py-3 sm:px-6">Grade</th><th className="px-5 py-3">Students</th><th className="px-5 py-3">Attendance rate</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 sm:px-6">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{monthlyAttendance.map((record) => { const isUpdated = record.status === "Updated" || updated.includes(record.grade); return <tr key={record.grade}><td className="px-5 py-4 font-semibold text-slate-800 sm:px-6">{record.grade}</td><td className="px-5 py-4 text-slate-600">{record.students}</td><td className="px-5 py-4 font-bold text-emerald-600">{record.rate}</td><td className="px-5 py-4"><Badge value={isUpdated ? "Updated" : "Pending"} /></td><td className="px-5 py-4 sm:px-6"><button onClick={() => setUpdated((current) => current.includes(record.grade) ? current : [...current, record.grade])} disabled={isUpdated} className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-semibold text-white disabled:bg-slate-100 disabled:text-slate-400">{isUpdated ? "Updated" : "Update month"}</button></td></tr> })}</tbody></table></div></Card></div> }

type Announcement = { id: number; title: string; audience: string; message: string; status: "Published" }
const initialAnnouncements: Announcement[] = [{ id: 1, title: "Parent–teacher meeting schedule", audience: "All parents", message: "Meeting slots for 26 September are now available in the parent portal.", status: "Published" }, { id: 2, title: "Term 3 fee deadline", audience: "Parents with pending fees", message: "Please complete fee payments by 15 September to avoid late charges.", status: "Published" }]

export const StaffAnnouncementsPage = () => { const [posts, setPosts] = useState(initialAnnouncements); const [title, setTitle] = useState(""); const [audience, setAudience] = useState("All parents"); const [message, setMessage] = useState(""); const [feedback, setFeedback] = useState(""); const publish = () => { if (!title.trim() || !message.trim()) { setFeedback("Enter both a title and message before publishing."); return }; setPosts((current) => [{ id: Date.now(), title, audience, message, status: "Published" }, ...current]); setTitle(""); setMessage(""); setFeedback("Announcement published successfully.") }; return <div className="mx-auto max-w-7xl space-y-6"><PageHeader title="Announcements" description="Publish school-wide updates for parents and staff portals." /><div className="grid gap-6 xl:grid-cols-5"><Card className="h-fit p-5 sm:p-6 xl:col-span-2"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><Megaphone className="size-5" /></span><div><h3 className="font-semibold text-slate-900">Create announcement</h3><p className="mt-0.5 text-sm text-slate-500">Select who can see this post.</p></div></div><label className="mt-6 block text-sm font-semibold text-slate-700">Audience<select value={audience} onChange={(event) => setAudience(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-500"><option>All parents</option><option>All staff</option><option>Parents & staff</option></select></label><label className="mt-4 block text-sm font-semibold text-slate-700">Title<input value={title} onChange={(event) => setTitle(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm outline-none focus:border-blue-500" placeholder="Enter announcement title" /></label><label className="mt-4 block text-sm font-semibold text-slate-700">Message<textarea value={message} onChange={(event) => setMessage(event.target.value)} className="mt-2 min-h-32 w-full rounded-lg border border-slate-200 p-3 text-sm outline-none focus:border-blue-500" placeholder="Write an important school update" /></label><button onClick={publish} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><Send className="size-4" /> Publish announcement</button>{feedback && <p className={`mt-4 rounded-lg px-3 py-2.5 text-sm font-medium ${feedback.startsWith("Enter") ? "bg-amber-50 text-amber-700" : "bg-emerald-50 text-emerald-700"}`}>{feedback}</p>}</Card><Card className="xl:col-span-3"><div className="border-b border-slate-100 px-5 py-5 sm:px-6"><h3 className="text-base font-semibold text-slate-900">Published announcements</h3><p className="mt-1 text-sm text-slate-500">Visible on connected parent and staff portals.</p></div><div className="divide-y divide-slate-100">{posts.map((post) => <article key={post.id} className="p-5 sm:px-6"><div className="flex items-start justify-between gap-4"><div><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{post.audience}</span><h4 className="mt-3 font-semibold text-slate-900">{post.title}</h4><p className="mt-1.5 text-sm leading-6 text-slate-500">{post.message}</p></div><Badge value={post.status} /></div></article>)}</div></Card></div></div> }

const initialHolidays = [{ id: 1, name: "Gandhi Jayanti", date: "02 October 2026", audience: "All students & staff" }, { id: 2, name: "Diwali break", date: "09–13 November 2026", audience: "All students & staff" }]

export const StaffHolidaysPage = () => { const [holidays, setHolidays] = useState(initialHolidays); const [published, setPublished] = useState(false); return <div className="mx-auto max-w-6xl space-y-6"><PageHeader title="School holidays" description="Publish the official holiday calendar to parent and staff portals." action={<button onClick={() => setPublished(true)} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><CalendarDays className="size-4" /> Publish calendar</button>} />{published && <div className="flex items-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700"><CheckCircle2 className="size-4" /> Holiday calendar published to all portals.</div>}<Card><div className="flex items-center justify-between border-b border-slate-100 px-5 py-5 sm:px-6"><div><h3 className="text-base font-semibold text-slate-900">Holiday calendar 2026–27</h3><p className="mt-1 text-sm text-slate-500">Parents and staff see published holidays immediately.</p></div><button onClick={() => setHolidays((current) => [...current, { id: Date.now(), name: "Winter break", date: "24–31 December 2026", audience: "All students & staff" }])} className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Add holiday</button></div><div className="divide-y divide-slate-100">{holidays.map((holiday) => <div key={holiday.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div><p className="font-semibold text-slate-800">{holiday.name}</p><p className="mt-1 text-sm text-slate-500">{holiday.date}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">{holiday.audience}</span></div>)}</div></Card></div> }

export const StaffNotificationsPage = () => { const [audience, setAudience] = useState("Parents with pending fees"); const [message, setMessage] = useState("Your Term 3 fee payment is due on 15 September. Please pay through the parent portal to avoid late charges."); const [sent, setSent] = useState(false); return <div className="mx-auto max-w-6xl space-y-6"><PageHeader title="Notifications" description="Send fee reminders, attendance updates, and other school messages." /><div className="grid gap-6 xl:grid-cols-5"><Card className="p-5 sm:p-6 xl:col-span-2"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><Bell className="size-5" /></span><div><h3 className="font-semibold text-slate-900">Send notification</h3><p className="mt-0.5 text-sm text-slate-500">Email and in-app delivery</p></div></div><label className="mt-6 block text-sm font-semibold text-slate-700">Recipients<select value={audience} onChange={(event) => setAudience(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-500"><option>Parents with pending fees</option><option>All parents</option><option>All staff</option><option>Grade 10 parents</option></select></label><label className="mt-4 block text-sm font-semibold text-slate-700">Message<textarea value={message} onChange={(event) => { setMessage(event.target.value); setSent(false) }} className="mt-2 min-h-32 w-full rounded-lg border border-slate-200 p-3 text-sm outline-none focus:border-blue-500" /></label><button onClick={() => setSent(true)} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><Send className="size-4" /> Send now</button>{sent && <p className="mt-4 rounded-lg bg-emerald-50 px-3 py-2.5 text-sm font-medium text-emerald-700">Notification sent to {audience}.</p>}</Card><Card className="xl:col-span-3"><div className="border-b border-slate-100 px-5 py-5 sm:px-6"><h3 className="text-base font-semibold text-slate-900">Delivery history</h3><p className="mt-1 text-sm text-slate-500">Latest communication from the office.</p></div><div className="divide-y divide-slate-100">{[["Term 3 fee reminder", "Parents with pending fees", "274 delivered"], ["Holiday calendar update", "All parents & staff", "1,207 delivered"], ["August attendance published", "All parents", "1,188 delivered"]].map(([title, recipients, delivery]) => <div key={title} className="flex items-center justify-between gap-4 p-5 sm:px-6"><div><p className="font-semibold text-slate-800">{title}</p><p className="mt-1 text-sm text-slate-500">{recipients}</p></div><div className="text-right"><p className="text-sm font-semibold text-emerald-600">{delivery}</p><p className="mt-1 text-xs text-slate-400">Sent today</p></div></div>)}</div></Card></div></div> }
